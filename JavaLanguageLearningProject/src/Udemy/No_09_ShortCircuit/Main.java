package Udemy.No_09_ShortCircuit;

public class Main {
    public static void main(String[] args) {
        /*
         * && || 會走捷徑
         * && 前面如果已經是 False, 後面就不會看了, 哪怕是一些會出錯的 statement
         * || 則是前面是 True, 後面就不會看了
         * 
         * 如果不希望做 short circuit, 則可以使用 & 跟 |
         */
    }
}
